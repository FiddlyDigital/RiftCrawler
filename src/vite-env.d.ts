/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Build version stamped by CI via Vite define (see vite.config.ts); "dev" in local builds. */
declare const __APP_VERSION__: string;
